package jaba.lists;

/**
 * we will try to reach java.util.List features
 */
public interface MyList {
    int size();
    boolean isEmpty();
    boolean contains(Object o);
    Object[] toArray();
}
